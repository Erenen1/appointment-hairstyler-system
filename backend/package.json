{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node dist/app.js",
    "start:prod": "NODE_ENV=production && node dist/app.js",
    "start:dev": "cross-env NODE_ENV=development nodemon src/app.ts",
    "dev": "nodemon -e ts --exec 'tsc -p ./tsconfig.json && node ./dist/app.js'",
    "build": "tsc",
    "build:prod": "NODE_ENV=production tsc",
    "test": "cross-env NODE_ENV=test jest",
    "test:watch": "cross-env NODE_ENV=test jest --watch",
    "test:coverage": "cross-env NODE_ENV=test jest --coverage",
    "test:ci": "cross-env NODE_ENV=test jest --ci --coverage --watchAll=false",
    "test:setup": "docker-compose -f docker-compose-test.yml up -d",
    "test:teardown": "docker-compose -f docker-compose-test.yml down",
    "test:full": "npm run test:setup && sleep 10 && npm run test:ci && npm run test:teardown",
    "swagger": "swagger-jsdoc -d swagger.config.js -o swagger.json",
    "migrate": "npx sequelize-cli db:migrate",
    "migrate:undo": "npx sequelize-cli db:migrate:undo",
    "migrate:status": "npx sequelize-cli db:migrate:status",
    "migrate:create": "npx sequelize-cli migration:generate --name",
    "test:dashboard": "cross-env NODE_ENV=test jest tests/controllers/dashboard.test.ts",
    "test:dashboard:watch": "cross-env NODE_ENV=test jest tests/controllers/dashboard.test.ts --watch",
    "test:dashboard:coverage": "cross-env NODE_ENV=test jest tests/controllers/dashboard.test.ts --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/multer": "^2.0.0",
    "cors": "^2.8.5",
    "date-fns": "^4.1.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "pg": "^8.16.0",
    "sequelize": "^6.37.7",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/date-fns": "^2.5.3",
    "@types/express": "^5.0.3",
    "@types/jest": "^29.5.14",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.0.3",
    "@types/pg": "^8.15.4",
    "@types/supertest": "^6.0.2",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.6",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.0.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
